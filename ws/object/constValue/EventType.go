package constValue

const (
	// CreateEvent the create event.
	CreateEvent = "Create"
	// UpdateEvent the update event.
	UpdateEvent = "Update"
	// DeleteEvent the delete event.
	DeleteEvent = "Delete"
)